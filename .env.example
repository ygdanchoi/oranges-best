# Database connection string (provided by Railway in production)
# For local development, use your local PostgreSQL URL
DATABASE_URL=postgresql://user:password@localhost:5432/orange_tierlist

# Master password for voting and admin access
MASTER_PASSWORD=orange

# Port (optional, defaults to 3000)
PORT=3000

# Node environment
NODE_ENV=development
